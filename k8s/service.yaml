apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
  namespace: fraud-detection

spec:
  selector:
    app: fraud-detection

  type: NodePort

  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080
